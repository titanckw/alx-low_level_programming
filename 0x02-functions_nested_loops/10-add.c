#include "main.h"

/**
 * add - Adds two integers and returns the result
 * @a: first integer
 * @b: second integer
 * Return: returns the results
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
